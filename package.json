{
  "name": "sam-rest",
  "version": "1.0.0",
  "description": "A REST-API based on SAM",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/index.js --bundle --outfile=public/index.js --minify --platform=node"
  },
  "repository": {
    "type": "git",
    "url": "github.com/DxsSucuk/SAM-REST"
  },
  "author": "Presti",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "sam-js": "^0.2.0",
    "tweetnacl-util": "^0.15.1"
  },
  "devDependencies": {
    "esbuild": "^0.15.12"
  }
}
